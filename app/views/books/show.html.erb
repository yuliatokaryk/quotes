<div class="max-w-sm rounded overflow-hidden shadow-lg">
  <div class="px-6 py-4">
    <div class="font-bold text-xl mb-2"><%= @book.title %></div>
    <p class="text-gray-700 text-base"><%= @book.year_of_publication %></p>
    <p class="text-gray-700 text-base"><%= @book.wikipedia_url %></p>
    <p class="text-gray-700 text-base"><%= @book.goodreads_url %></p>
    <p class="text-gray-700 text-base"><%= @book.author.full_name %></p>
  </div>
</div>
